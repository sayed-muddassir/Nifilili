spring:
  application:
    name: nifilili

  datasource:
    url: jdbc:postgresql://localhost:5432/nifilili_db
    username: sayedmuddassirhussain
    password: 060398
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

logging:
  level:
    org.flywaydb: INFO